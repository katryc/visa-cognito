
<<<<<<< HEAD
<div class="input-field col s12">
   <select>
     <option value="" disabled selected>Choose your option</option>
     <option value="1">Unlock for 15 mins</option>
     <option value="2">Unlock for 1 hour</option>
     <option value="3">Unlock for 3 hours</option>
   </select>
   <label>Unlock Timer</label>
 </div>
=======
<div id="wrap">
  <form action="" id="rateform" onsubmit="return false;">
    <div>
      <div class="cont_order">
        <fieldset>


          <br/>
          <label >Select Time Allowance</label>

          <select id="condition" name='condition' onchange="calculateTotal()">
            <option value="None">Select time allowance for E-Commerce purchases.</option>
            <option value="15min">15 minutes</option>
             <%= ctc_post(wait_time) %>
            <option value="anhour">1 hour</option>
            <%= ctc_post(wait_time) %>
            <option value="3hours">3 hours</option>
            <%= ctc_post(wait_time) %>
            <br/>
          </select>
          <br/>
          <p>

<button onclick="myFunction()">Unlock</button>

<script>
function myFunction() {
    alert("Your credit card is now unlocked for e-commerce transactions.");
}





//Set up an associative array
//The keys represent the condition type
//The value represents the cost of the condition i.e. Intercounty condition is $5,Dobash condition is $9
//We use this this array when the user selects a condition from the form
var wait_time = new Array();
wait_time["15min"]=15;
wait_time["anhour"]=60;
wait_time["3hours"]=180;



function getMuvrPrice()
{
  var wait_time_int=0;
  //Get a reference to the form id="rateform"
  var theForm = document.forms["rateform"];

  var selectedpath = theForm.elements["selectedpath"];
  //Here since there are 4 radio buttons selectedpath.length = 4
  //We loop through each radio buttons
  for(var i = 0; i < selectedpath.length; i++)
  {
    //if the radio button is checked
    if(selectedpath[i].checked)
    {
      //we set MuvrTypePrice to the value of the selected radio button
      //i.e. if the user choose the 8" cake we set it to 25
      //by using the muvr_prices array
      //We get the selected Items value
      //For example muvr_prices["Round8".value]"
      wait_time_int = wait_time[selectedpath[i].value];
      //If we get a match then we break out of this loop
      //No reason to continue if we get a match
      break;
    }
  }
  //We return the MuvrTypePrice
  return wait_time_int;
}
</script>
>>>>>>> 28538d0e1bbc4dedcde896031078b551c42fa906
